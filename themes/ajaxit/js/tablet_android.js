//alert( 'android tablet. ANDROID TABLET NOT FORMATTED TO WORK YET... at all... try a mobile instead.');

/*
function setxOrientation() {
	//var supportsOrientationChange = "onorientationchange" in window, orientationEvent = supportsOrientationChange ? "orientationchange" : "resize";
	//alert('HOLY ROTATING SCREENS BATMAN:' + window.orientation + " " + screen.width);
	//window.addEventListener(orientationEvent, function() {
	//    alert('HOLY ROTATING SCREENS BATMAN:' + window.orientation + " " + screen.width);
	//}, false);
	
	
	
	xOrientation = Math.abs(window.orientation) == 90 ? 'landscape' : 'portrait';
	xwidth = window.innerWidth;
	xheight = window.innerHeight;
	//alert( xwidth +', '+ xheight );
	//setTimeout("alert( window.innerWidth +', '+ window.innerHeight );", 1500);
}


window.onresize = function(event) {
    window_resize(event);
}

//timeout wrapper points with doResizeCode as callback
function window_resize(e) { 
	//window.clearTimeout(resizeTimeoutId); 
	//resizeTimeoutId = window.setTimeout('doResizeCode();', 10);
	setTimeout('doResizeCode();', 10);
}

//wrapper for height/width check
function doResizeCode() {
    if(window.innerHeight > window.innerWidth){
		xOrientation = 'portrait';
        //alert("Please view in landscape");
    } else {
		xOrientation = 'landscape';
	}
	alert( xOrientation );
}
*/




