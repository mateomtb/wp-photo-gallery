        {% for post in columnists[count][:1] %}
    <figure>
        <a href="{{post.link}}"><img alt="" src="{{post.thumbnail.src}}"></a>
        <hgroup>
            <h5><a href="{{post.category.link}}">{{post.category}}</a></h5>
            <h3><a href="{{post.link}}">{{post.post_title}}</a></h3>
        </hgroup>
    </figure>
    {% endfor %}

